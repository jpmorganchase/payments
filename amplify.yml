version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd client
        - yarn install
    build:
      commands:
        - cd client
        - yarn run build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /client/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
