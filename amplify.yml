version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd app
        - cd client
        - yarn install
    build:
      commands:
        - yarn run build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /app/client/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
